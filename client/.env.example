# Leave VITE_API_URL empty to build the static "demo" mode (browser calls
# Open-Meteo directly and stores data in localStorage). Set it to the API
# origin to use the Node.js backend, e.g. https://weather-wiz-api.onrender.com
VITE_API_URL=
