{
  "name": "@weatherwiz/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Open-Meteo DTOs, normalisers, URL builders and validation schemas shared by the API and the browser demo mode.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "vitest": "^3.2.0"
  }
}
